using System;
using System.Collections.Generic;
using System.IO;
using System.Linq;
using System.Runtime.Remoting.Contexts;
using System.Text;
using System.Threading.Tasks;
using static System.Net.Mime.MediaTypeNames;
using System.Xml.Linq;

namespace Homework_8
{
    internal class Program
    {
        static void Main(string[] args)
        {
            //            8 Simple file explorer:
            //            a.Create a new Console Application project in C#.
            //b. Implement a method to list the contents of a directory using Directory.GetFiles() and Directory.GetDirectories().
            //c.Add methods for basic file operations like copy, move, delete, and rename using File.Copy(), File.Move(), File.Delete(), and Directory.Move().
            //d.Create a simple user interface allowing users to navigate their file system and perform the available file operations.
            //e.Handle exceptions and errors related to file access, permissions, and invalid paths.
        }
    }
}
